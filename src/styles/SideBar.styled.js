import styled from 'styled-components';

export const StyledSideBar = styled.div`
    height: 100%
    width: 250px;
    background-color: #ffa500;
    position: fixed;
    left: 0;
    transform: translateX(0); /* Center the sidebar horizontally */
    top: 50%; /* Position the sidebar at the top of the viewport */
`